## Durable memory log – 2026-06-04 08:13 EDT

- Debi reported difficulties adding new folders with `git add .` — the new files were not staged; assistant explained that Git only tracks files, not empty directories, and that .gitignore may be blocking the path. 
- Assistant provided diagnostic steps: `git status`, `git check-ignore`, adjusting .gitignore, adding placeholder `.gitkeep`, and forcing add with `-f`.
- Debi noted reluctance to overload app.js and discussed possibility of keeping module‑specific loaders in their own JS files; assistant suggested event‑delegation pattern.
- Assistant apologized for accidental large internal dump.
- Discussed Git commit workflow: stage, commit, push.
- User reported that the button to load the tutorial component of the Earn submodule is not working; decided to pause and push current changes to GitHub for the trip.
- Encountered a commit issue where new folders weren't added despite `git add .`; discussed possible reasons (empty folders, .gitignore).
